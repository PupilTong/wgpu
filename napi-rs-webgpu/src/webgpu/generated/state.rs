//! The fixed-function state a render pipeline is assembled from, and the
//! attachments a render pass writes.
//!
//! Generated by `tools/generate.py` from `tools/surface.json`. Do not edit by
//! hand: change the spec or the generator and re-run it.

use super::enums::{
    GpuBlendFactor, GpuBlendOperation, GpuCompareFunction, GpuCullMode, GpuFrontFace,
    GpuIndexFormat, GpuLoadOp, GpuPrimitiveTopology, GpuStencilOperation, GpuStoreOp,
    GpuTextureFormat,
};
use super::geometry::GpuColorDict;
use super::layouts::GpuVertexBufferLayout;
use super::pipeline::GpuShaderModule;
use super::resources::GpuTextureView;

js_type! {
    /// The WebGPU `GPUBlendComponent` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBlendComponent: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBlendComponent;
    /// Construct a new `GpuBlendComponent`.
    new new();
    /// Change the `dstFactor` field of this object.
    setter set_dst_factor(val: GpuBlendFactor) as "dstFactor";
    /// Change the `operation` field of this object.
    setter set_operation(val: GpuBlendOperation) as "operation";
    /// Change the `srcFactor` field of this object.
    setter set_src_factor(val: GpuBlendFactor) as "srcFactor";
}

webgpu_default!(GpuBlendComponent, new);

js_type! {
    /// The WebGPU `GPUBlendState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBlendState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBlendState;
    /// Construct a new `GpuBlendState`.
    new new(alpha: &GpuBlendComponent as "alpha", color: &GpuBlendComponent as "color");
}

js_type! {
    /// The WebGPU `GPUColorTargetState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuColorTargetState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuColorTargetState;
    /// Construct a new `GpuColorTargetState`.
    new new(format: GpuTextureFormat as "format");
    /// Change the `blend` field of this object.
    setter set_blend(val: &GpuBlendState) as "blend";
    /// Change the `writeMask` field of this object.
    setter set_write_mask(val: u32) as "writeMask";
}

js_type! {
    /// The WebGPU `GPUDepthStencilState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuDepthStencilState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuDepthStencilState;
    /// Construct a new `GpuDepthStencilState`.
    new new(format: GpuTextureFormat as "format");
    /// Change the `depthBias` field of this object.
    setter set_depth_bias(val: i32) as "depthBias";
    /// Change the `depthBiasClamp` field of this object.
    setter set_depth_bias_clamp(val: f32) as "depthBiasClamp";
    /// Change the `depthBiasSlopeScale` field of this object.
    setter set_depth_bias_slope_scale(val: f32) as "depthBiasSlopeScale";
    /// Change the `depthCompare` field of this object.
    setter set_depth_compare(val: GpuCompareFunction) as "depthCompare";
    /// Change the `depthWriteEnabled` field of this object.
    setter set_depth_write_enabled(val: bool) as "depthWriteEnabled";
    /// Change the `stencilBack` field of this object.
    setter set_stencil_back(val: &GpuStencilFaceState) as "stencilBack";
    /// Change the `stencilFront` field of this object.
    setter set_stencil_front(val: &GpuStencilFaceState) as "stencilFront";
    /// Change the `stencilReadMask` field of this object.
    setter set_stencil_read_mask(val: u32) as "stencilReadMask";
    /// Change the `stencilWriteMask` field of this object.
    setter set_stencil_write_mask(val: u32) as "stencilWriteMask";
}

js_type! {
    /// The WebGPU `GPUFragmentState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuFragmentState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuFragmentState;
    /// Construct a new `GpuFragmentState`.
    new new(
        module: &GpuShaderModule as "module",
        targets: &[crate::JsOption<GpuColorTargetState>] as "targets",
    );
    /// Change the `entryPoint` field of this object.
    setter set_entry_point(val: &str) as "entryPoint";
}

js_type! {
    /// The WebGPU `GPUMultisampleState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuMultisampleState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuMultisampleState;
    /// Construct a new `GpuMultisampleState`.
    new new();
    /// Change the `alphaToCoverageEnabled` field of this object.
    setter set_alpha_to_coverage_enabled(val: bool) as "alphaToCoverageEnabled";
    /// Change the `count` field of this object.
    setter set_count(val: u32) as "count";
    /// Change the `mask` field of this object.
    setter set_mask(val: u32) as "mask";
}

webgpu_default!(GpuMultisampleState, new);

js_type! {
    /// The WebGPU `GPUPrimitiveState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuPrimitiveState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuPrimitiveState;
    /// Construct a new `GpuPrimitiveState`.
    new new();
    /// Change the `cullMode` field of this object.
    setter set_cull_mode(val: GpuCullMode) as "cullMode";
    /// Change the `frontFace` field of this object.
    setter set_front_face(val: GpuFrontFace) as "frontFace";
    /// Change the `stripIndexFormat` field of this object.
    setter set_strip_index_format(val: GpuIndexFormat) as "stripIndexFormat";
    /// Change the `topology` field of this object.
    setter set_topology(val: GpuPrimitiveTopology) as "topology";
    /// Change the `unclippedDepth` field of this object.
    setter set_unclipped_depth(val: bool) as "unclippedDepth";
}

webgpu_default!(GpuPrimitiveState, new);

js_type! {
    /// The WebGPU `GPUProgrammableStage` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuProgrammableStage: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuProgrammableStage;
    /// Construct a new `GpuProgrammableStage`.
    new new(module: &GpuShaderModule as "module");
    /// Change the `entryPoint` field of this object.
    setter set_entry_point(val: &str) as "entryPoint";
}

js_type! {
    /// The WebGPU `GPURenderPassColorAttachment` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderPassColorAttachment: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderPassColorAttachment;
    /// Construct a new `GpuRenderPassColorAttachment`.
    new new_with_gpu_texture_view(
        load_op: GpuLoadOp as "loadOp",
        store_op: GpuStoreOp as "storeOp",
        view: &GpuTextureView as "view",
    );
    /// Change the `clearValue` field of this object.
    setter set_clear_value_gpu_color_dict(val: &GpuColorDict) as "clearValue";
    /// Change the `resolveTarget` field of this object.
    setter set_resolve_target_gpu_texture_view(val: &GpuTextureView) as "resolveTarget";
    /// Change the `storeOp` field of this object.
    setter set_store_op(val: GpuStoreOp) as "storeOp";
}

js_type! {
    /// The WebGPU `GPURenderPassDepthStencilAttachment` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderPassDepthStencilAttachment: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderPassDepthStencilAttachment;
    /// Construct a new `GpuRenderPassDepthStencilAttachment`.
    new new_with_gpu_texture_view(view: &GpuTextureView as "view");
    /// Change the `depthClearValue` field of this object.
    setter set_depth_clear_value(val: f32) as "depthClearValue";
    /// Change the `depthLoadOp` field of this object.
    setter set_depth_load_op(val: GpuLoadOp) as "depthLoadOp";
    /// Change the `depthReadOnly` field of this object.
    setter set_depth_read_only(val: bool) as "depthReadOnly";
    /// Change the `depthStoreOp` field of this object.
    setter set_depth_store_op(val: GpuStoreOp) as "depthStoreOp";
    /// Change the `stencilClearValue` field of this object.
    setter set_stencil_clear_value(val: u32) as "stencilClearValue";
    /// Change the `stencilLoadOp` field of this object.
    setter set_stencil_load_op(val: GpuLoadOp) as "stencilLoadOp";
    /// Change the `stencilReadOnly` field of this object.
    setter set_stencil_read_only(val: bool) as "stencilReadOnly";
    /// Change the `stencilStoreOp` field of this object.
    setter set_stencil_store_op(val: GpuStoreOp) as "stencilStoreOp";
}

js_type! {
    /// The WebGPU `GPUStencilFaceState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuStencilFaceState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuStencilFaceState;
    /// Construct a new `GpuStencilFaceState`.
    new new();
    /// Change the `compare` field of this object.
    setter set_compare(val: GpuCompareFunction) as "compare";
    /// Change the `depthFailOp` field of this object.
    setter set_depth_fail_op(val: GpuStencilOperation) as "depthFailOp";
    /// Change the `failOp` field of this object.
    setter set_fail_op(val: GpuStencilOperation) as "failOp";
    /// Change the `passOp` field of this object.
    setter set_pass_op(val: GpuStencilOperation) as "passOp";
}

webgpu_default!(GpuStencilFaceState, new);

js_type! {
    /// The WebGPU `GPUVertexState` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuVertexState: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuVertexState;
    /// Construct a new `GpuVertexState`.
    new new(module: &GpuShaderModule as "module");
    /// Change the `buffers` field of this object.
    setter set_buffers(val: &[crate::JsOption<GpuVertexBufferLayout>]) as "buffers";
    /// Change the `entryPoint` field of this object.
    setter set_entry_point(val: &str) as "entryPoint";
}
