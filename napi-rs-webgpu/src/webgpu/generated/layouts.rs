//! The dictionaries describing a binding: what a bind group holds, what a bind
//! group layout permits, and how vertex buffers are read.
//!
//! Generated by `tools/generate.py` from `tools/surface.json`. Do not edit by
//! hand: change the spec or the generator and re-run it.

use super::enums::{
    GpuBufferBindingType, GpuSamplerBindingType, GpuStorageTextureAccess, GpuTextureFormat,
    GpuTextureSampleType, GpuTextureViewDimension, GpuVertexFormat, GpuVertexStepMode,
};
use super::resources::{GpuBuffer, GpuSampler, GpuTextureView};

js_type! {
    /// The WebGPU `GPUBindGroupEntry` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBindGroupEntry: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBindGroupEntry;
    /// Construct a new `GpuBindGroupEntry`.
    new new(binding: u32 as "binding", resource: &GpuSampler as "resource");
    /// Construct a new `GpuBindGroupEntry`.
    new new_with_gpu_buffer_binding(
        binding: u32 as "binding",
        resource: &GpuBufferBinding as "resource",
    );
    /// Construct a new `GpuBindGroupEntry`.
    new new_with_gpu_texture_view(
        binding: u32 as "binding",
        resource: &GpuTextureView as "resource",
    );
}

js_type! {
    /// The WebGPU `GPUBindGroupLayoutEntry` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBindGroupLayoutEntry: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBindGroupLayoutEntry;
    /// Construct a new `GpuBindGroupLayoutEntry`.
    new new(binding: u32 as "binding", visibility: u32 as "visibility");
    /// Change the `buffer` field of this object.
    setter set_buffer(val: &GpuBufferBindingLayout) as "buffer";
    /// Change the `externalTexture` field of this object.
    setter set_external_texture(val: &GpuExternalTextureBindingLayout) as "externalTexture";
    /// Change the `sampler` field of this object.
    setter set_sampler(val: &GpuSamplerBindingLayout) as "sampler";
    /// Change the `storageTexture` field of this object.
    setter set_storage_texture(val: &GpuStorageTextureBindingLayout) as "storageTexture";
    /// Change the `texture` field of this object.
    setter set_texture(val: &GpuTextureBindingLayout) as "texture";
}

js_type! {
    /// The WebGPU `GPUBufferBinding` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBufferBinding: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBufferBinding;
    /// Construct a new `GpuBufferBinding`.
    new new(buffer: &GpuBuffer as "buffer");
    /// Change the `buffer` field of this object.
    setter set_buffer(val: &GpuBuffer) as "buffer";
    /// Change the `offset` field of this object.
    setter set_offset_f64(val: f64) as "offset";
    /// Change the `size` field of this object.
    setter set_size_f64(val: f64) as "size";
}

js_type! {
    /// The WebGPU `GPUBufferBindingLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBufferBindingLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBufferBindingLayout;
    /// Construct a new `GpuBufferBindingLayout`.
    new new();
    /// Change the `hasDynamicOffset` field of this object.
    setter set_has_dynamic_offset(val: bool) as "hasDynamicOffset";
    /// Change the `minBindingSize` field of this object.
    setter set_min_binding_size_f64(val: f64) as "minBindingSize";
    /// Change the `type` field of this object.
    setter set_type(val: GpuBufferBindingType) as "type";
}

webgpu_default!(GpuBufferBindingLayout, new);

js_type! {
    /// The WebGPU `GPUExternalTextureBindingLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuExternalTextureBindingLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuExternalTextureBindingLayout;
    /// Construct a new `GpuExternalTextureBindingLayout`.
    new new();
}

webgpu_default!(GpuExternalTextureBindingLayout, new);

js_type! {
    /// The WebGPU `GPUSamplerBindingLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuSamplerBindingLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuSamplerBindingLayout;
    /// Construct a new `GpuSamplerBindingLayout`.
    new new();
    /// Change the `type` field of this object.
    setter set_type(val: GpuSamplerBindingType) as "type";
}

webgpu_default!(GpuSamplerBindingLayout, new);

js_type! {
    /// The WebGPU `GPUStorageTextureBindingLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuStorageTextureBindingLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuStorageTextureBindingLayout;
    /// Construct a new `GpuStorageTextureBindingLayout`.
    new new(format: GpuTextureFormat as "format");
    /// Change the `access` field of this object.
    setter set_access(val: GpuStorageTextureAccess) as "access";
    /// Change the `format` field of this object.
    setter set_format(val: GpuTextureFormat) as "format";
    /// Change the `viewDimension` field of this object.
    setter set_view_dimension(val: GpuTextureViewDimension) as "viewDimension";
}

js_type! {
    /// The WebGPU `GPUTextureBindingLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuTextureBindingLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuTextureBindingLayout;
    /// Construct a new `GpuTextureBindingLayout`.
    new new();
    /// Change the `multisampled` field of this object.
    setter set_multisampled(val: bool) as "multisampled";
    /// Change the `sampleType` field of this object.
    setter set_sample_type(val: GpuTextureSampleType) as "sampleType";
    /// Change the `viewDimension` field of this object.
    setter set_view_dimension(val: GpuTextureViewDimension) as "viewDimension";
}

webgpu_default!(GpuTextureBindingLayout, new);

js_type! {
    /// The WebGPU `GPUVertexAttribute` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuVertexAttribute: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuVertexAttribute;
    /// Construct a new `GpuVertexAttribute`.
    new new_with_f64(
        format: GpuVertexFormat as "format",
        offset: f64 as "offset",
        shader_location: u32 as "shaderLocation",
    );
    /// Change the `format` field of this object.
    setter set_format(val: GpuVertexFormat) as "format";
    /// Change the `offset` field of this object.
    setter set_offset_f64(val: f64) as "offset";
}

js_type! {
    /// The WebGPU `GPUVertexBufferLayout` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuVertexBufferLayout: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuVertexBufferLayout;
    /// Construct a new `GpuVertexBufferLayout`.
    new new_with_f64(
        array_stride: f64 as "arrayStride",
        attributes: &[GpuVertexAttribute] as "attributes",
    );
    /// Change the `stepMode` field of this object.
    setter set_step_mode(val: GpuVertexStepMode) as "stepMode";
}
