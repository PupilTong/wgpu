//! The dictionaries that describe an object about to be created.
//!
//! Generated by `tools/generate.py` from `tools/surface.json`. Do not edit by
//! hand: change the spec or the generator and re-run it.

use super::device::GpuDevice;
use super::enums::{
    GpuAddressMode, GpuAutoLayoutMode, GpuCanvasAlphaMode, GpuCanvasToneMappingMode,
    GpuCompareFunction, GpuFilterMode, GpuMipmapFilterMode, GpuPowerPreference, GpuTextureAspect,
    GpuTextureDimension, GpuTextureFormat, GpuTextureViewDimension,
};
use super::geometry::GpuExtent3dDict;
use super::layouts::{GpuBindGroupEntry, GpuBindGroupLayoutEntry};
use super::pipeline::GpuPipelineLayout;
use super::query::{GpuComputePassTimestampWrites, GpuRenderPassTimestampWrites};
use super::resources::GpuBindGroupLayout;
use super::state::{
    GpuDepthStencilState, GpuFragmentState, GpuMultisampleState, GpuPrimitiveState,
    GpuProgrammableStage, GpuRenderPassColorAttachment, GpuRenderPassDepthStencilAttachment,
    GpuVertexState,
};

js_type! {
    /// The WebGPU `GPUBindGroupDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBindGroupDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBindGroupDescriptor;
    /// Construct a new `GpuBindGroupDescriptor`.
    new new(
        entries: &[GpuBindGroupEntry] as "entries",
        layout: &GpuBindGroupLayout as "layout",
    );
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUBindGroupLayoutDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBindGroupLayoutDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBindGroupLayoutDescriptor;
    /// Construct a new `GpuBindGroupLayoutDescriptor`.
    new new(entries: &[GpuBindGroupLayoutEntry] as "entries");
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUBufferDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuBufferDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuBufferDescriptor;
    /// Construct a new `GpuBufferDescriptor`.
    new new_with_f64(size: f64 as "size", usage: u32 as "usage");
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `mappedAtCreation` field of this object.
    setter set_mapped_at_creation(val: bool) as "mappedAtCreation";
    /// Change the `size` field of this object.
    setter set_size_f64(val: f64) as "size";
    /// Change the `usage` field of this object.
    setter set_usage(val: u32) as "usage";
}

js_type! {
    /// The WebGPU `GPUCanvasConfiguration` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuCanvasConfiguration: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuCanvasConfiguration;
    /// Construct a new `GpuCanvasConfiguration`.
    new new(device: &GpuDevice as "device", format: GpuTextureFormat as "format");
    /// Change the `alphaMode` field of this object.
    setter set_alpha_mode(val: GpuCanvasAlphaMode) as "alphaMode";
    /// Change the `format` field of this object.
    setter set_format(val: GpuTextureFormat) as "format";
    /// Change the `toneMapping` field of this object.
    setter set_tone_mapping(val: &GpuCanvasToneMapping) as "toneMapping";
    /// Change the `usage` field of this object.
    setter set_usage(val: u32) as "usage";
    /// Change the `viewFormats` field of this object.
    setter set_view_formats(val: &[crate::JsString]) as "viewFormats";
}

js_type! {
    /// The WebGPU `GPUCanvasToneMapping` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuCanvasToneMapping: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuCanvasToneMapping;
    /// Construct a new `GpuCanvasToneMapping`.
    new new();
    /// Change the `mode` field of this object.
    setter set_mode(val: GpuCanvasToneMappingMode) as "mode";
}

webgpu_default!(GpuCanvasToneMapping, new);

js_type! {
    /// The WebGPU `GPUCommandBufferDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuCommandBufferDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuCommandBufferDescriptor;
    /// Construct a new `GpuCommandBufferDescriptor`.
    new new();
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

webgpu_default!(GpuCommandBufferDescriptor, new);

js_type! {
    /// The WebGPU `GPUCommandEncoderDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuCommandEncoderDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuCommandEncoderDescriptor;
    /// Construct a new `GpuCommandEncoderDescriptor`.
    new new();
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

webgpu_default!(GpuCommandEncoderDescriptor, new);

js_type! {
    /// The WebGPU `GPUComputePassDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuComputePassDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuComputePassDescriptor;
    /// Construct a new `GpuComputePassDescriptor`.
    new new();
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `timestampWrites` field of this object.
    setter set_timestamp_writes(val: &GpuComputePassTimestampWrites) as "timestampWrites";
}

webgpu_default!(GpuComputePassDescriptor, new);

js_type! {
    /// The WebGPU `GPUComputePipelineDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuComputePipelineDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuComputePipelineDescriptor;
    /// Construct a new `GpuComputePipelineDescriptor`.
    new new(
        layout: &GpuPipelineLayout as "layout",
        compute: &GpuProgrammableStage as "compute",
    );
    /// Construct a new `GpuComputePipelineDescriptor`.
    new new_with_gpu_auto_layout_mode(
        layout: GpuAutoLayoutMode as "layout",
        compute: &GpuProgrammableStage as "compute",
    );
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUDeviceDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuDeviceDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuDeviceDescriptor;
    /// Construct a new `GpuDeviceDescriptor`.
    new new();
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `requiredFeatures` field of this object.
    setter set_required_features(val: &[crate::JsString]) as "requiredFeatures";
    /// Change the `requiredLimits` field of this object.
    setter set_required_limits(val: &crate::Object<crate::Number>) as "requiredLimits";
}

webgpu_default!(GpuDeviceDescriptor, new);

js_type! {
    /// The WebGPU `GPUExternalTextureDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuExternalTextureDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuExternalTextureDescriptor;
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUObjectDescriptorBase` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuObjectDescriptorBase: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuObjectDescriptorBase;
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUPipelineDescriptorBase` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuPipelineDescriptorBase: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuPipelineDescriptorBase;
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUPipelineLayoutDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuPipelineLayoutDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuPipelineLayoutDescriptor;
    /// Construct a new `GpuPipelineLayoutDescriptor`.
    new new(bind_group_layouts: &[crate::JsOption<GpuBindGroupLayout>] as "bindGroupLayouts");
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUQueueDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuQueueDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuQueueDescriptor;
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPURenderBundleDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderBundleDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderBundleDescriptor;
    /// Construct a new `GpuRenderBundleDescriptor`.
    new new();
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

webgpu_default!(GpuRenderBundleDescriptor, new);

js_type! {
    /// The WebGPU `GPURenderBundleEncoderDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderBundleEncoderDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderBundleEncoderDescriptor;
    /// Construct a new `GpuRenderBundleEncoderDescriptor`.
    new new(color_formats: &[crate::JsOption<crate::JsString>] as "colorFormats");
    /// Change the `depthReadOnly` field of this object.
    setter set_depth_read_only(val: bool) as "depthReadOnly";
    /// Change the `depthStencilFormat` field of this object.
    setter set_depth_stencil_format(val: GpuTextureFormat) as "depthStencilFormat";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `sampleCount` field of this object.
    setter set_sample_count(val: u32) as "sampleCount";
    /// Change the `stencilReadOnly` field of this object.
    setter set_stencil_read_only(val: bool) as "stencilReadOnly";
}

js_type! {
    /// The WebGPU `GPURenderPassDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderPassDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderPassDescriptor;
    /// Construct a new `GpuRenderPassDescriptor`.
    new new(
        color_attachments: &[crate::JsOption<GpuRenderPassColorAttachment>] as "colorAttachments",
    );
    /// Change the `depthStencilAttachment` field of this object.
    setter set_depth_stencil_attachment(
        val: &GpuRenderPassDepthStencilAttachment,
    ) as "depthStencilAttachment";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `timestampWrites` field of this object.
    setter set_timestamp_writes(val: &GpuRenderPassTimestampWrites) as "timestampWrites";
}

js_type! {
    /// The WebGPU `GPURenderPipelineDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRenderPipelineDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRenderPipelineDescriptor;
    /// Construct a new `GpuRenderPipelineDescriptor`.
    new new(layout: &GpuPipelineLayout as "layout", vertex: &GpuVertexState as "vertex");
    /// Construct a new `GpuRenderPipelineDescriptor`.
    new new_with_gpu_auto_layout_mode(
        layout: GpuAutoLayoutMode as "layout",
        vertex: &GpuVertexState as "vertex",
    );
    /// Change the `depthStencil` field of this object.
    setter set_depth_stencil(val: &GpuDepthStencilState) as "depthStencil";
    /// Change the `fragment` field of this object.
    setter set_fragment(val: &GpuFragmentState) as "fragment";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `multisample` field of this object.
    setter set_multisample(val: &GpuMultisampleState) as "multisample";
    /// Change the `primitive` field of this object.
    setter set_primitive(val: &GpuPrimitiveState) as "primitive";
}

js_type! {
    /// The WebGPU `GPURequestAdapterOptions` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuRequestAdapterOptions: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuRequestAdapterOptions;
    /// Construct a new `GpuRequestAdapterOptions`.
    new new();
    /// Change the `powerPreference` field of this object.
    setter set_power_preference(val: GpuPowerPreference) as "powerPreference";
}

webgpu_default!(GpuRequestAdapterOptions, new);

js_type! {
    /// The WebGPU `GPUSamplerDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuSamplerDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuSamplerDescriptor;
    /// Construct a new `GpuSamplerDescriptor`.
    new new();
    /// Change the `addressModeU` field of this object.
    setter set_address_mode_u(val: GpuAddressMode) as "addressModeU";
    /// Change the `addressModeV` field of this object.
    setter set_address_mode_v(val: GpuAddressMode) as "addressModeV";
    /// Change the `addressModeW` field of this object.
    setter set_address_mode_w(val: GpuAddressMode) as "addressModeW";
    /// Change the `compare` field of this object.
    setter set_compare(val: GpuCompareFunction) as "compare";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `lodMaxClamp` field of this object.
    setter set_lod_max_clamp(val: f32) as "lodMaxClamp";
    /// Change the `lodMinClamp` field of this object.
    setter set_lod_min_clamp(val: f32) as "lodMinClamp";
    /// Change the `magFilter` field of this object.
    setter set_mag_filter(val: GpuFilterMode) as "magFilter";
    /// Change the `maxAnisotropy` field of this object.
    setter set_max_anisotropy(val: u16) as "maxAnisotropy";
    /// Change the `minFilter` field of this object.
    setter set_min_filter(val: GpuFilterMode) as "minFilter";
    /// Change the `mipmapFilter` field of this object.
    setter set_mipmap_filter(val: GpuMipmapFilterMode) as "mipmapFilter";
}

webgpu_default!(GpuSamplerDescriptor, new);

js_type! {
    /// The WebGPU `GPUShaderModuleCompilationHint` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuShaderModuleCompilationHint: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuShaderModuleCompilationHint;
    /// Change the `entryPoint` field of this object.
    setter set_entry_point(val: &str) as "entryPoint";
}

js_type! {
    /// The WebGPU `GPUShaderModuleDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuShaderModuleDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuShaderModuleDescriptor;
    /// Construct a new `GpuShaderModuleDescriptor`.
    new new(code: &str as "code");
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
}

js_type! {
    /// The WebGPU `GPUTextureDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuTextureDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuTextureDescriptor;
    /// Construct a new `GpuTextureDescriptor`.
    new new_with_gpu_extent_3d_dict(
        format: GpuTextureFormat as "format",
        size: &GpuExtent3dDict as "size",
        usage: u32 as "usage",
    );
    /// Change the `dimension` field of this object.
    setter set_dimension(val: GpuTextureDimension) as "dimension";
    /// Change the `format` field of this object.
    setter set_format(val: GpuTextureFormat) as "format";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `mipLevelCount` field of this object.
    setter set_mip_level_count(val: u32) as "mipLevelCount";
    /// Change the `sampleCount` field of this object.
    setter set_sample_count(val: u32) as "sampleCount";
    /// Change the `usage` field of this object.
    setter set_usage(val: u32) as "usage";
    /// Change the `viewFormats` field of this object.
    setter set_view_formats(val: &[crate::JsString]) as "viewFormats";
}

js_type! {
    /// The WebGPU `GPUTextureViewDescriptor` dictionary.
    ///
    /// A dictionary is a plain object with no class of its own, so the cast
    /// tests for an object rather than for a constructor.
    GpuTextureViewDescriptor: [crate::Object, crate::JsValue],
    instanceof(value) { value.is_object() },
}

webgpu_dictionary! {
    GpuTextureViewDescriptor;
    /// Construct a new `GpuTextureViewDescriptor`.
    new new();
    /// Change the `arrayLayerCount` field of this object.
    setter set_array_layer_count(val: u32) as "arrayLayerCount";
    /// Change the `aspect` field of this object.
    setter set_aspect(val: GpuTextureAspect) as "aspect";
    /// Change the `baseArrayLayer` field of this object.
    setter set_base_array_layer(val: u32) as "baseArrayLayer";
    /// Change the `baseMipLevel` field of this object.
    setter set_base_mip_level(val: u32) as "baseMipLevel";
    /// Change the `dimension` field of this object.
    setter set_dimension(val: GpuTextureViewDimension) as "dimension";
    /// Change the `format` field of this object.
    setter set_format(val: GpuTextureFormat) as "format";
    /// Change the `label` field of this object.
    setter set_label(val: &str) as "label";
    /// Change the `mipLevelCount` field of this object.
    setter set_mip_level_count(val: u32) as "mipLevelCount";
    /// Change the `usage` field of this object.
    setter set_usage(val: u32) as "usage";
}

webgpu_default!(GpuTextureViewDescriptor, new);
